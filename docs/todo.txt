- "2 min ago"
- Verifier que ce sont bien les X *derniers* qui remontent dans la liste au chargement
- Remplacer # par le 1ere partie de l'uuid
- Mettre la date au format de la date du browser
- Tenter de reactiver la connexion SSE dès qu'elle a coupé


## Search string

- Query: status=200 AND (method=POST OR method=GET)
- Sauvegardée dans le navigateur (cookie)
- Quand je tape une string, au blur / entrée, filtre la liste
- Au rechargement de la page, si query dans cookie, fill le input et filtre la liste
- Autocomplete
